---
- name: Create docker network
  community.docker.docker_network:
    name: "{{ docker_network_name }}"
    driver: bridge
    state: present

- name: Install MakeMKV
  include_tasks: "mkv.yml"

- name: Install Handbrake
  include_tasks: "handbrake.yml"
