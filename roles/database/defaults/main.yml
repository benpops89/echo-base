---
docker_network_name: database
postgres_version: 18.0-alpine3.22

postgres_users:
  - name: kestra
    password: "{{ kestra_db_password }}"
  - name: readeck
    password: "{{ readeck_db_password }}"

postgres_databases:
  - name: kestra
    owner: kestra
    comment: Metadata database for Kestra
    encoding: UTF-8
  - name: readeck
    owner: readeck
    comment: Database for readeck
    encoding: UTF-8
